# OS X Scala Setup

# Assumes: 
# + Homebrew has been installed correctly.

brew install scala -with-docs     # installs scala, fsc, scalac, scaladoc, scalap
brew install sbt       # build tool for Java and Scala
brew install maven     # project management
brew install thrift    # cross-language interface definition language 